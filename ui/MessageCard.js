import React from "react";
import { Text, View } from "react-native";

const MessageCard = ({ picture, displayName, username, content }) => (
  <View>
    <Text>MessageCard</Text>
  </View>
);

export default MessageCard;
